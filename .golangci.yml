run:
  timeout: 2m

linters:
  disable-all: true
  enable:
    - gofmt
    - gofumpt
    - gosimple
    - gocyclo
    - misspell
    - govet

linters-settings:
  gofumpt:
    extra-rules: true
  goimports:
    local-prefixes: github.com/amit3512/descheduler_policy_master
